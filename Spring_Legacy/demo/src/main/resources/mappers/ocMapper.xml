<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mappers.ocMapper">

    <select id="alllistoc" parameterType="hashMap" resultType="com.example.demo.domain.ocVO">
        SELECT * FROM SPRING_TB
    </select>
</mapper>